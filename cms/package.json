{
  "name": "cms",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env dev --local-protocol=https",
    "wrangler:deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "migrate": "wrangler d1 migrations apply content_db",
    "migrate:remote": "wrangler d1 migrations apply content_db --remote"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.952.0",
    "@aws-sdk/s3-request-presigner": "^3.952.0",
    "hono": "^4.11.1",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
